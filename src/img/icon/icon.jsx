/* eslint-disable react/prop-types */
import icon from '../../img/sprite.svg'

export const IconUser = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconUsers`}></use>
	</svg>
)

export const IconAutomatic = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconAutomatic`}></use>
	</svg>
)

export const IconAC = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconAC`}></use>
	</svg>
)

export const IconPetrol = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconPetrol`}></use>
	</svg>
)

export const IconKitchen = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconKitchen`}></use>
	</svg>
)

export const IconBeds = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconBeds`}></use>
	</svg>
)

export const IconAirConditioner = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconAirConditioner`}></use>
	</svg>
)

export const IconCD = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconCD`}></use>
	</svg>
)

export const IconRadio = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconRadio`}></use>
	</svg>
)

export const IconHob = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconHob`}></use>
	</svg>
)

export const IconToilet = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconToilet`}></use>
	</svg>
)

export const IconShower = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconShower`}></use>
	</svg>
)

export const IconFreezer = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconFreezer`}></use>
	</svg>
)

export const IconMdiGas = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconMdiGas`}></use>
	</svg>
)

export const IconWater = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconWater`}></use>
	</svg>
)

export const IconMicrowave = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconMicrowave`}></use>
	</svg>
)

export const IconRoute = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconRoute`}></use>
	</svg>
)

export const IconCalendar = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconCalendar`}></use>
	</svg>
)

export const IconTV = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconTV`}></use>
	</svg>
)

export const IconVan = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconCamper`}></use>
	</svg>
)

export const IconFullyIntegrated = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconCamper1`}></use>
	</svg>
)

export const IconAlcove = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconCamper2`}></use>
	</svg>
)

export const IconHeart = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconHeart`}></use>
	</svg>
)

export const IconHeartFilled = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconHeartFilled`}></use>
	</svg>
)

export const IconMapPin = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconMapPin`}></use>
	</svg>
)
